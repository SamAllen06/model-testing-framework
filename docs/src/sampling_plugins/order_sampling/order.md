# Order
APP/src/sampling_plugins/order_sampling/order.py

## Purpose
An Order is a type of [SampleGroup](../../sampling/sample_group.md) that reads
instructions from a json file and uses them to generate samples.