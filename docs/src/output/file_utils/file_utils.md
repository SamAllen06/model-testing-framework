# File Utils 
APP/src/output/file_utils/

## Purpose
File utils contains submodules that implement common functionality used by
modules that require file output. This can involve converting data into a stream
able to be written to a file, or, in the case of [FileSystemTree](file_system_tree.md),
storing a number of streams in a tree that can later be written directly to the
file system.
