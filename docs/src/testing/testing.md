# Testing
APP/src/testing/

## Purpose
The testing module contains all the modules used in the process of testing the
model. Testing consists of writing samples to the model's input file, running
the model, and reading in the resulting data from the model.
