# Binary Runner
APP/src/testing/binary_runner.py

## Purpose
The BinaryRunner class, as the name would imply, is designed to run a binary
(the model.) It can also be configured to pass arguments to the model using
[the main config file.](../../config/main.md)
